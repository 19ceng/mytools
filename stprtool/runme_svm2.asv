%% Example: SVM2
% Burada `data`, 2 boyutlu feature ve 2 farkli sinifli 250 
clear all;  close all;  clc;
data = load('riply_trn');

options = struct('ker','rbf','arg',1,'C',1);
model = svm2(data,options )

figure(1); ppatterns(data);
figure(2); ppatterns(data); psvm( model );